# AIGCTool 使用说明

发行包解压后双击 **`AIGCTool.exe`**（Windows）或 **`AIGCTool`**（macOS）打开窗口；源码用户可运行 `启动.bat`。顶部显示下载工具是否就绪，首次使用点击「安装 / 更新下载工具」。

---

## 一、拆分 / 合并

### 拆分
1. 把视频文件或文件夹**拖进**「拆分」子页（或用按钮选择）。拖文件夹会自动批量处理里面所有视频。
2. 选模式：
   - **按固定时长**：填秒数（如 `30`），从头到尾每 30 秒一段。
   - **按段数**：填段数（如 `5`），平均切成约 5 段。
3. 点「开始拆分」。结果在每个源视频旁的 `<视频名>_片段/` 文件夹里，命名 `001 002 …`。

### 合并
1. 把多个视频拖进「合并」子页（顺序 = 列表从上到下）。
2. 用「↑ 上移 / ↓ 下移」调顺序。
3. 点「开始合并」，选保存位置。

### 关于"无损"和切点
- 为保证片段能**逐帧还原**，切分只在**关键帧**处下刀。所以每段实际时长会**贴近**你填的秒数但不一定精确到秒（取决于关键帧间隔）。这是"不重编码 + 可还原"的必然取舍。
- 合并要求各视频编码/分辨率/参数一致（本工具自己拆出来的片段天然满足）。

---

## 二、多分辨率导出

1. 选「源视频」。
2. 勾选要导出的分辨率（4K / 1080 / 720，默认全选）。
3. 选编码：
   - **h265**（默认）：交付成片，体积小、观感无损。可调 CRF（越小越清晰、越大越省空间，默认 18）、preset、可勾「GPU 加速」用 NVIDIA NVENC。
   - **prores**：ProRes 422 HQ 母版，体积巨大，用于二次剪辑/调色。
4. 输出目录留空 = 源文件旁的 `output/`。
5. 点「开始导出」。统一锁 25fps CFR、16:9、Lanczos 缩放、BT.709。

---

## 三、视频下载

1. 在文本框里**每行粘一个视频链接**（B站 / YouTube 自动识别，可批量）。含播放列表参数的 YouTube 视频链接只下载当前视频。
2. 需要登录或网站要求验证时，选择自己导出的 **Netscape 格式 cookies.txt**。文件可以保留原名称，包含中文、空格或 `(3)` 都可以。留空只尝试公开访问，不会自动选用旧文件，也不会读取浏览器数据库。
3. 选择本机保存目录。若需指定网络代理，填写自己的地址，例如 `http://127.0.0.1:7890`；端口以自己的代理软件设置为准，留空使用系统设置。
4. 点击「开始下载」。程序选择最高可用画质和最佳音轨，合并为 MP4；完成后日志显示实际分辨率、时长、音轨和文件位置。不会通过重新编码或超分制造更高分辨率。

### 导出自己的 Cookie

- 从浏览器官方扩展商店安装可信的 Cookie 导出扩展，例如「Get cookies.txt LOCALLY」，仅导出对应视频网站的数据。
- B站：在自己的浏览器登录并确认视频可播放，再导出网站 Cookie。
- YouTube：公开视频也可能要求“登录以确认不是机器人”。可在新的无痕窗口登录 YouTube，在同一标签页打开 `https://www.youtube.com/robots.txt`，导出 `youtube.com` 的 Cookie，然后关闭该无痕窗口，减少会话被后续浏览轮换的影响。参见 [yt-dlp 官方说明](https://github.com/yt-dlp/yt-dlp/wiki/Extractors#exporting-youtube-cookies)。
- Cookie 是登录凭证，只在自己的电脑中选择使用；不要把 Cookie 文件提交到 GitHub、发送给别人或上传到空马服务器。下载器只会把相应网站的 Cookie 用于向该视频网站发起请求。
- 批量链接使用同一个 Cookie 文件；不同网站需要分开下载并选择各自的文件。

### 下载失败时

- **登录验证 / 不是机器人**：先在本机浏览器确认能播放，再重新导出并选择 Cookie。文件能读取不代表登录仍有效，更新下载器也不会自动修复 Cookie。
- **JavaScript 验证失败**：点击「安装 / 更新下载工具」，确保有受支持的 Node.js 22+ 或 Deno 2.3+。程序会自动把检测到的运行时传给 yt-dlp。
- **超时 / 连接失败**：检查本机网络及代理；浏览器能播放时，也要确认下载器使用的代理设置一致。
- **403 / 权限不足**：查看日志中的具体原因，检查该账号的视频访问权限、网络和 Cookie；不能仅凭 403 判断 Cookie 已过期。
- **工具过时**：点击「安装 / 更新下载工具」。该按钮在工具已安装后仍可使用；已校验的新工具才会替换旧版本。

所有下载都在本机完成，不需要注册空马账号或部署下载服务器。MP4 容器中的视频可能是 AV1 / VP9，音频可能是 Opus；若剪辑软件不支持，可使用支持这些编码的播放器，或另行转换兼容格式。

---

## 四、屏幕录制

1. 「选择区域」框选录制范围，或「全屏」。
2. 选 FPS（15/24/30/60）和保存目录。
3. 点「● 开始录制」（窗口会自动最小化让出画面）；再点「■ 停止录制」结束。
4. **全局热键**：`Ctrl+F9` 开始/停止，`Ctrl+F10` 暂停/继续——录制时窗口最小化也能用。
5. 录完弹窗可一键打开所在文件夹。

> 录制输出为 mp4（mp4v 编码）。如需更高画质/更小体积，可把录好的视频拖到「多分辨率导出」再转一次。

---

## 常见问题

- **顶部显示工具缺失**：点击「安装 / 更新下载工具」。安装完成后状态和下载按钮会自动刷新。
- **下载页是灰的**：缺少 yt-dlp，先用顶部按钮安装，无需安装 Python。
- **拖拽没反应**：`tkinterdnd2` 没装好，可改用按钮选择文件；或重跑 `启动.bat` 重装依赖。
- **启动闪退**：看本目录下的 `crash.log`。
